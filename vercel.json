{
    "version": 2,
    "buildCommand": "npx prisma generate --schema=server/prisma/schema.prisma && npm run build:api && vite build",
    "outputDirectory": "dist",
    "framework": "vite",
    "rewrites": [
        { "source": "/api/(.*)", "destination": "/api/index.js" },
        { "source": "/(.*)", "destination": "/index.html" }
    ]
}