{
    "installCommand": "npm install && cd server && npm install",
    "buildCommand": "cd server && npx prisma generate && npx esbuild src/vercel-entry.ts --bundle --platform=node --format=esm --packages=external --outfile=../api/index.js && cd .. && npm run build",
    "outputDirectory": "dist",
    "framework": "vite",
    "rewrites": [
        { "source": "/api/(.*)", "destination": "/api/index" },
        { "source": "/(.*)", "destination": "/index.html" }
    ]
}