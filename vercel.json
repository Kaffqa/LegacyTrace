{
    "installCommand": "npm install",
    "buildCommand": "cd server && npm install && cd .. && npx prisma@6.4.1 generate --schema=server/prisma/schema.prisma && cd server && npx esbuild src/vercel-entry.ts --bundle --platform=node --format=esm --packages=external --outfile=../api/index.js && cd .. && npm run build",
    "outputDirectory": "dist",
    "framework": "vite",
    "rewrites": [
        { "source": "/api/(.*)", "destination": "/api/index.js" },
        { "source": "/(.*)", "destination": "/index.html" }
    ]
}