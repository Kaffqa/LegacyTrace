{
    "installCommand": "npm install",
    "buildCommand": "cd server && npm install && npx prisma generate && npx esbuild src/vercel-entry.ts --bundle --platform=node --format=esm --packages=external --outfile=../api/index.js && cd .. && npm run build",
    "outputDirectory": "dist",
    "framework": "vite",
    "rewrites": [
        { "source": "/api/(.*)", "destination": "/api/index.js" },
        { "source": "/(.*)", "destination": "/index.html" }
    ]
}